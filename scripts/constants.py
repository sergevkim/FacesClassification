from pathlib import Path


PATHS = {
    'original_indices_labels_filename': f"{Path.cwd()}/data/list_attr_celeba.txt",
    'new_original_indices_bijection_filename': f"{Path.cwd()}/data/image_list.txt",
    'new_indices_labels_filename': f"{Path.cwd()}/data/labels.txt",
}

